import { useState } from 'react';

const Login = () => {
  // state
  // state

  const handleSubmit = async((event) => {
    // api call
  });

  return (
    <div>
      <p>log in</p>
    </div>
  );
};

export default Login;
