import { useState } from 'react';

const SignUp = () => {
  // state
  // state

  const handleSubmit = async((event) => {
    // api call
  });

  return (
    <div>
      <p>sign up</p>
    </div>
  );
};

export default SignUp;
